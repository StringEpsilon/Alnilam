
if (process.env.NODE_ENV === "development") {
	// tslint:disable-next-line:no-console
	console.warn("NavLink has been merged with Link. The export will be dropped in the future.");
}
import Link from "./components/Link";
export default Link;
